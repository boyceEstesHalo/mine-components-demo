    import XCTest
    @testable import MineComponentsDemo

    final class MineComponentsDemoTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(MineComponentsDemo().text, "Hello, World!")
        }
    }
